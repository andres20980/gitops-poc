apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-byebyeworld
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-byebyeworld
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-byebyeworld
    spec:
      containers:
        - name: byebyeworld
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
