apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-helloworld-service
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ .Release.Name }}-helloworld
