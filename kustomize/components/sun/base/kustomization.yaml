# kustomize/components/sun/base/kustomization.yaml v1.3
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Using the simplified local path after refactoring the directory structure.
helmCharts:
  - name: ../../../charts/helm-base
    releaseName: sun
    valuesFile: values.yaml

labels:
- pairs:
    app.kubernetes.io/component: sun
  includeSelectors: true
