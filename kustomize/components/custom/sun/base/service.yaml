# kustomize/components/sun/base/service.yaml v1.1.0
# This is the ACTIVE service for the Blue-Green strategy.
# It points to the stable (Blue) version and handles all production traffic.
apiVersion: v1
kind: Service
metadata:
  name: sun-service
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: sun