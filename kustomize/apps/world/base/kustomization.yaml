# kustomize/apps/world/base/kustomization.yaml v1.2
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../components/helloworld/base
  - ../../../components/moon/base
  - ../../../components/carbone/base

# Updated 'commonLabels' to the new 'labels' syntax to remove warnings.
labels:
- pairs:
    app.kubernetes.io/part-of: world-app
  includeSelectors: true
