📁 Directory structure for './shared-services'
./shared-services
└── carbone
    ├── Chart.yaml
    ├── common-values.yaml
    ├── values-dev.yaml
    ├── values-pre.yaml
    └── values.yaml

2 directories, 5 files

📄 File contents within './shared-services' (recursive)

==== ./shared-services/carbone/values.yaml ====
# Valores base para carbone (puedes poner aquí los valores comunes a todos los entornos)
chart-base:
  nameOverride: carbone
  app:
    registry: ghcr.io/andres20980
    image: carbone
    version: local-v1
  replicaCount: 1
  service:
    type: ClusterIP
    port: 8080
    targetPort: 8080
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
  env:
    LOG_LEVEL: INFO

==== ./shared-services/carbone/Chart.yaml ====
apiVersion: v2
name: carbone
description: Helm Chart for the Carbone component
type: application
version: 0.1.0
appVersion: "local-v1"

dependencies:
  - name: chart-base
    version: "1.2.1"
    repository: "oci://ghcr.io/andres20980" # Repositorio OCI donde se encuentran los charts

==== ./shared-services/carbone/values-dev.yaml ====
# Valores específicos para desarrollo
chart-base:
  app:
    version: local-v1
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

==== ./shared-services/carbone/values-pre.yaml ====
# Valores específicos para preproducción
chart-base:
  app:
    version: pre-build
  replicaCount: 2
  resources:
    limits:
      cpu: 300m
      memory: 384Mi
    requests:
      cpu: 150m
      memory: 192Mi

==== ./shared-services/carbone/common-values.yaml ====
# Valores comunes para todos los entornos (si hay alguna variable global para carbone)
chart-base:
  env:
    LOG_LEVEL: INFO
