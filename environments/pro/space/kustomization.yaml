# environments/pro/space/kustomization.yaml v1.0.0
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: space-pro
resources:
  - ../../../apps/space # Inherit from the app composition

# Apply a patch to all deployments within this app for this environment
patches:
  - path: patch-replicas.yaml
